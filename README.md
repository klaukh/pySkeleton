## Python Package Skeleton (pySkeleton)

1) A `bash` script that will create a `python` package skeleton

usage:

    pySkeleton.sh mypackage

2) A sample package skeleton generated from the included `bash` script

